// Defining function
function getsum(num1,num2){
   var total=num1+num2;
   return total;
}
// Calling function
document.write(getsum(5,6));

/*The displaySum() function in the following example takes two numbers as arguments, 
simply add them together and then display the result in the browser.*/

